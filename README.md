In this project we were asked to create our own _printf() function, that replicates the standard printf() function. This project was to be done in a collaborative way ,a group of two (pair programming).
